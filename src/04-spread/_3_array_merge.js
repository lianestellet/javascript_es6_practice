const defaultColors = ['red', 'blue', 'green']
const userDefinedColors = ['yellow', 'orange']

const mergedColors = [...defaultColors, ...userDefinedColors]

console.log('Merged colors', mergedColors)